# ── Qdrant ────────────────────────────────────────────────────────────────────
# LOCAL  (Docker Compose / bare metal): set HOST + PORT, leave QDRANT_URL blank
# CLOUD  (Heroku / DO App Platform)   : set QDRANT_URL + QDRANT_API_KEY only
QDRANT_URL=                            # https://xyz.qdrant.io  (Qdrant Cloud)
QDRANT_HOST=localhost                  # ignored when QDRANT_URL is set
QDRANT_PORT=6333                       # ignored when QDRANT_URL is set
QDRANT_API_KEY=                        # required for Qdrant Cloud
QDRANT_COLLECTION=orpheus_songs
FRAME_COLLECTION=orpheus_frames

# ── Server ────────────────────────────────────────────────────────────────────
# Heroku / DO App Platform inject PORT automatically — leave blank locally
# PORT=8000

# ── Ingestion scheduler ───────────────────────────────────────────────────────
SONGS_PER_RUN=4
SCHEDULE_MINUTES=30

# ── Spotify (primary song source) ───────────────────────────────────────────────────
# Get credentials at https://developer.spotify.com/dashboard
# Without these, ingestion falls back to the YouTube query pool.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_COUNTRY=US

# ── Frame extraction windows ──────────────────────────────────────────────────
FRAME_WINDOW_S=5.0
FRAME_HOP_S=2.5

# ── Audio ─────────────────────────────────────────────────────────────────────
SCRATCH_DIR=./scratch
# Windows only — Linux/Mac/Docker have ffmpeg on PATH via apt/brew
# FFMPEG_PATH=C:/Users/User/ffmpeg/bin/ffmpeg.exe
